# 身份指纹系统 · 远程名册方案 Y · 部署运行手册

> 配套设计文档：`身份指纹系统_远程名册方案Y_技术设计.md`
> 适用：cs-a（learning-platform）、ss-a（sales-platform）、tc-a（teacher-console）三站。
> 目标：老师用「老师密码」在任意电脑纯网页签发激活码 → 即时写入学生站 KV → 学生秒级激活，学生站应用代码不再因新增学生而重部署。

---

## 0. 工作原理（一句话）

老师密码 ──PBKDF2──▶ Ed25519 私钥（浏览器内派生，不出浏览器）──签名──▶ `POST /api/issue`
→ Cloudflare Function 用 `TEACHER_PUBLIC_KEY` 验签 → 写入 KV → 学生站 `GET /api/certs` 拉取 → `verifyCert(内置公钥)` 激活。

旧学生（用旧随机密钥签发）不因本方案失效：其证书仍在 `public.json` 数组与打包 `certs.json` 中，平滑过渡。

---

## 1. 一次性本地引导（生成并写入公钥）

在 `teacher-console/` 目录执行（需 Node 20+）：

```bash
cd teacher-console
npm run bootstrap "<你的老师密码>"
# 等价于：node tools/bootstrap.mjs "<你的老师密码>"
```

脚本会：
1. 用密码派生 Ed25519 公钥（私钥只在内存，不落盘）。
2. 把派生公钥追加写入三站 `src/data/public.json`（保留原随机密钥，数组形态）。
   - 若 cs-a/ss-a 仓库不在本地同级目录，会提示你手动把打印出的公钥粘进其 `public.json` 数组。
3. 终端打印 **`TEACHER_PUBLIC_KEY` 的值**（一串公钥 JWK）—— 下一步要用。
4. 生成 `tools/kv-seed.json`（两站已有证书合并），供可选 KV 种子使用。

⚠️ 密码即根信任：丢失密码无法再签发新码；改密码 = 换密钥 = 需重新引导并重部署。请用强密码。

---

## 2. Cloudflare 后台配置（每个**学生站**各做一次：cs-a、ss-a）

### 2.1 建 KV 命名空间并绑定
- 控制台 → **Workers & Pages** → 选学生站项目 → **Settings → Variables → KV namespaces**。
- 新建命名空间（如 `certs-kv`）。
- 绑定名填 **`CERTS_KV`**（Production 与 Preview 都勾选）。
- 绑定后**重新部署一次**该学生站，Function 才能读到 `CERTS_KV`。

### 2.2 设环境变量 `TEACHER_PUBLIC_KEY`
- 同一页面 **Environment variables** → 新增：
  - 变量名：`TEACHER_PUBLIC_KEY`
  - 值：第 1 步终端打印的那串公钥 JWK（明文变量，公钥非密）
  - 作用域：Production + Preview
- （可选）若希望 Function 也能验旧随机密钥签发的证书，把旧公钥也并入数组后粘贴；通常只需派生公钥。

### 2.3 （可选）KV 种子——让远程名册也含旧学生
不填也行：学生站会兜底用打包的 `certs.json` 激活旧学生。若要让 `/api/certs` 也含旧学生：
```bash
# 在 teacher-console/ 下，需已登录 Cloudflare（wrangler login）
npx wrangler kv key put --binding CERTS_KV certs --path tools/kv-seed.json
```
每个学生站绑定的 `CERTS_KV` 都写一次；或在 KV 控制台手动新建键 `certs`，值为 `kv-seed.json` 内容。

---

## 3. 部署三站

引导改了 `public.json`、新增了 `functions/`，需重新部署：
- **cs-a / ss-a**：推送仓库触发 Pages 自动部署（或控制台手动 Deploy）。`functions/api/certs.js` 与 `functions/api/issue.js` 会被识别为 Function（注意位置见 §5）。
- **tc-a**：同样重新部署即可，**无需任何 Cloudflare 环境变量或 KV 绑定**。tc-a 是纯静态前端：密钥在浏览器内由密码派生、公钥内置 `public.json`、签发请求直接 `POST` 到学生站的 `/api/issue`；`TEACHER_PUBLIC_KEY` 与 `CERTS_KV` 只存在于学生站（cs-a/ss-a）的 Function 环境。环境变量与 KV 仅学生站需要，tc-a 不必配。

---

## 4. 日常使用（纯网页）

1. 老师开 `tc-a.pages.dev` → 面板①「网页签发并发布到远程名册（方案 Y）」。
2. 输入老师密码 + 选目标课程站（cs-a / ss-a）+ 填写名册（每行 `学号,姓名`）。
3. 点「签发并发布」：浏览器派生密钥 → 生成激活码并签名 → `POST /api/issue` → 写入 KV。
4. 激活码自动存入本机「激活码库」（供之后 HMAC 核验学生导出）。
5. 学生端开 `cs-a/ss-a` → 身份激活 → 输入学号+姓名+激活码 → 秒级从 KV 拉到证书并验签通过。

---

## 5. 部署位置注意（关键）

Cloudflare Pages 的 `functions/` 目录必须位于 **Pages 项目的构建根目录**：
- 若 Pages 项目 Root directory 设为 `learning-platform`（cs-a）/ `sales-platform`（ss-a），则 `functions/` 放在该目录内（已实现：`learning-platform/functions/`、`sales-platform/functions/`）。
- 若你的 Pages Root directory 设为仓库根，则 `functions/` 必须放在仓库根（与 `learning-platform/` 同级），并已相应移动本方案的两个文件。
- 两端必须一致，否则 Function 不会生效（学生站会回退到打包名册，旧学生仍可激活，只是新发码不即时）。

---

## 6. 安全与费用

- 端点 `/api/issue` 无需密码：只接受用「老师私钥」签过的证书，外人无私钥签不进假证书。
- 远程名册不含激活码明文（仅 `sid,name,sig`）。
- CORS 对 `*` 放开：因端点只收有效签名，无敏感信息暴露，属可接受权衡（如需收紧可改为仅 `https://tc-a.pages.dev`）。
- 费用：KV 免费额度 10 万读/1 千写/天/1GB；一个班用量是免费的万分之一，无收费风险。
- 密码 = 根信任的固有权衡见设计文档 §7、§11。

---

## 7. 故障排查

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| 老师端「密码错误」 | 密码与引导时不一致 | 用引导时的密码；或重新 `npm run bootstrap` 同一密码 |
| 学生激活提示「名册找不到」 | KV 未绑定/未部署 Function | 检查 `CERTS_KV` 绑定 + 已部署；学生站会回退打包名册（旧学生可用） |
| 新发码学生仍激活不了 | Function 未生效 / CORS | 见 §5 位置；浏览器控制台看 `/api/issue` 返回 |
| 改了密码后旧发码失效 | 换了密钥 | 重新引导 + 重部署三站；已激活学生（旧公钥验过）仍可被核验 |
