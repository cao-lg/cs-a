# 身份指纹系统 · 远程名册方案 Y · 技术设计文档

> 状态：设计稿（待实现）。适用站点：cs-a / ss-a 学生站 + tc-a 教师端。
> 定位：把"教师派发激活码"从「本机脚本 + 重部署学生站」改为「纯网页签发 + 边缘存储即时生效」，学生端无需因新增学生而重部署。
> 这是一套**通用方案**：任何"零后端、需防冒用/防篡改、又要浏览器端签发"的激活码/ enrollment 场景都可复用。

---

## 1. 背景与要解决的问题

当前实现里，学生端 `IdentityGate` 用 `import certs from '../data/certs.json'` 把证书名单**打进应用包**。
后果：老师每给一名新生签发激活码，新证书只写入源码 `certs.json`，**学生站必须重新 build + 部署**，新生才能激活。老师觉得"分离不彻底"。

痛点归纳：
- 派发与部署耦合：发一个码 = 动一次应用构建。
- 老师希望"纯网页、没电脑也能发码" → 本机 `issue-codes.mjs` 不满足。
- 希望"派发后学生端立即生效，不重部署"。

---

## 2. 设计目标

| 目标 | 说明 |
|------|------|
| 分离部署 | 派发新生后，学生站**应用代码不重部署**，仅数据（名册）更新 |
| 即时生效 | 学生端能在秒级拿到最新名册并激活（方案 Y 走 KV，非方案 X 的 git 重建） |
| 纯网页签发 | 老师在任意联网电脑开 `tc-a.pages.dev` 输密码即可发码，无需本机脚本/私钥文件 |
| 零/极低费用 | 仅用 Cloudflare Pages Function + KV，免费额度内（见 §8） |
| 保持密码学防冒用 | 保留 Ed25519 证书签名；远程名册即使被篡改也不影响验真 |
| 跨电脑便携 | 签发密钥由"老师密码"派生，不落盘、不随文件传输 |

---

## 3. 方案总览（架构）

```
┌─────────────────────────┐        ① 输密码(浏览器派生密钥) + 名册
│  tc-a 教师端 (网页)       │──────────▶ 浏览器内 Ed25519 签名 (密码不出浏览器)
└─────────────────────────┘
            │ ② POST /api/issue  {sid,name,code,sig}   (密码不发送)
            ▼
┌─────────────────────────┐
│  Cloudflare Pages Function│  ③ 用 TEACHER_PUBLIC_KEY 验签名 → 通过才写
│  + KV 绑定               │  ④ 读 KV[certs] → 追加合并 → 写回 KV
└─────────────────────────┘
            │ ⑤ KV 即时更新
            ▼
┌─────────────────────────┐        ⑥ 学生激活: GET /api/certs (Function 读 KV)
│  cs-a / ss-a 学生站       │──────────▶ 拿到最新名册 → verifyCert(内置公钥, 学生输入)
│  (应用代码不变)           │            通过即锁定身份, 可导出
└─────────────────────────┘
            │ ⑦ 学生导出 .json (含 identity+sig+mac)
            ▼
┌─────────────────────────┐        ⑧ tc-a 导入导出 → 用公钥验归属 + 激活码库验 HMAC
│  tc-a 教师端 (核验/分析)   │
└─────────────────────────┘
```

要点：学生站的**应用代码一次部署后不再变**；变化的是 KV 里的 `certs` 数据。

---

## 4. 密码学设计

沿用现有 `src/lib/identity.js`（Web Crypto，浏览器/Node 通用）：
- 证书：`cert = sign(priv, 学号|姓名|激活码)`，公钥内置 App 验签，无法伪造新身份。
- 导出防篡改：`k = SHA256(激活码)`，`bundleMac` 把记录绑死在身份上。

**新增：老师密码派生密钥（跨电脑、不落盘）**
- `seed = PBKDF2-HMAC-SHA256(password, SALT, 迭代≥200000)`
- `priv = importEd25519(seed)`，`pub = deriveFrom(priv)`
- `SALT` 为提交在仓库的公开常量（KDF 参数，非秘密；用于保证不同电脑派生结果一致）。
- 公钥 `pub` 一次性提交到：学生站 `public.json`（打包进应用）+ Function 环境变量 `TEACHER_PUBLIC_KEY`。
- **密码即根信任**：忘记密码 = 无法再签发新码（已激活学生导出仍用旧公钥验过，照常可核验）；改密码 = 换密钥 = 需重新部署学生站公钥 + 更新 Function 环境变量。

> 替代（不推荐）：把私钥直接塞前端 → 任何人扒 JS 可提取伪造，绝对禁止。

---

## 5. 各组件职责与数据格式

### 5.1 KV 存储
- 命名空间：`certs-kv`（每个学生站独立，或共享并按站点前缀区分）。
- 键：`certs` → 值 = JSON 数组，结构与现有 `certs.json` 一致：
  ```json
  [ { "sid": "2024001", "name": "张三", "sig": "..." }, ... ]
  ```
- **不含激活码明文**（与现状一致）：激活码只由老师本地持有、学生本人知道。远程名册被截获也看不到任何激活码。

### 5.2 学生站 `public.json`（打包进应用）
- 老师公钥 JWK（由密码派生）。学生激活时用于 `verifyCert`。
- 仅换密钥时才需重部署。

### 5.3 Pages Function（两个路由）
- `GET /api/certs`：读 KV `certs` 返回 JSON（可加 `Cache-Control: public, max-age=30`）。**公开只读**。
- `POST /api/issue`：请求体为证书数组 `[{sid,name,code,sig}, ...]`。
  - 逐条用 `TEACHER_PUBLIC_KEY` 验 `verifyCert(pub, {sid,name,code,sig})`。
  - 全部通过 → 读 KV `certs`，按 `sid` 去重追加，写回。返回成功。
  - 任一不通过 → 拒绝整批（422）。
- **端点可不设密码**：因为只接受"用老师私钥签过"的证书，外人签不进假证书。可选再加一个共享 secret 头防垃圾写入。

### 5.4 tc-a 教师端签发 UI（新增）
- 输入：老师密码 + 名册（学号,姓名，可粘贴/文本框）。
- 行为：浏览器内 PBKDF2 派生密钥 → 每行为其生成随机激活码 → 签名 → `POST /api/issue`。
- 同时把 `{sid,name,code}` 提供"保存到本机激活码库"（用于后续 HMAC 核验导出），沿用现有"保存激活码库"逻辑。

---

## 6. 通信流程

### 6.1 派发（老师，纯网页）
1. 老师开 `tc-a`，输入密码 + 名册。
2. 浏览器 PBKDF2 派生 Ed25519 密钥（密码不出浏览器）。
3. 每行生成随机激活码 `code`，计算 `sig = sign(priv, sid|name|code)`。
4. `POST /api/issue` 发送 `[{sid,name,code,sig}]`（**密码不发送**）。
5. Function 验签 → 合并写 KV。即时生效。

### 6.2 学生激活（学生站，无需重部署）
1. 学生输入 学号+姓名+激活码。
2. 应用 `GET /api/certs` 拉最新名册（Function 读 KV）。
3. 查 `certs` 是否有 `sid+name`；有则 `verifyCert(publicKey, {sid,name,code,sig})`。
4. 通过 → 锁定本机身份，可学习/导出。

### 6.3 教师核验导出（tc-a）
1. 老师导入学生导出 `.json`。
2. Function/前端用内置公钥逐把试（cs-a/ss-a 双公钥）验归属。
3. 用本地"激活码库"的 `code` 复核 HMAC 防篡改。
4. 进入分析报表。（此部分沿用现有逻辑，不变。）

---

## 7. 安全模型与威胁分析

| 威胁 | 现状是否抵御 | 说明 |
|------|--------------|------|
| 学生伪造身份 | ✅ | 需老师私钥签名；公钥验不过即拒 |
| 篡改导出文件 | ✅ | HMAC 绑死记录，老师端复核 |
| 远程名册被篡改/替换 | ✅ | 每条证书仍须真私钥签名，假证书验不过；学生激活不受影响（仅可用性风险：名册被删则暂时查不到，可回滚 KV） |
| 公钥被替换（学生站拉到假公钥） | ⚠️ | 学生站公钥**打包内置**，不从网络拉，故不受影响；若将来改为远程拉公钥需加 pinning |
| 老师密码泄露 | ⚠️ | 泄露者可用老师身份发有效码；缓解=强密码+密码管理器；这是"密码=根信任"的固有权衡 |
| Function 端点被刷 | ✅ 低风险 | 只接受有效签名证书；加 `Cache-Control` + 可选 secret 头即可控成本 |
| 激活码在远程名册泄露 | ✅ | 远程名册**不含激活码明文**（仅 sid,name,sig） |

结论：方案 Y 在"零后端"前提下，保留了原有的防冒用/防篡改强度；新增风险主要是"老师密码=根信任"，靠强密码缓解。

---

## 8. Cloudflare 配置与费用

### 8.1 资源
- **Pages Function**：每个学生站加 `functions/api/certs.js`（GET）、`functions/api/issue.js`（POST）。
- **KV 命名空间**：`certs-kv`，绑到学生站 Pages 项目（绑定名如 `CERTS_KV`）。
- **环境变量/密钥**：
  - `TEACHER_PUBLIC_KEY`（普通变量，公钥非密）—— Function 验签用。
  - KV 绑定 `CERTS_KV` —— Function 读写用。
  - （可选）`ISSUE_SECRET` —— 防垃圾写入的共享头。

### 8.2 免费额度（2026-08 核实）
- Workers/Pages Function：**10 万请求/天**，10ms CPU/请求。
- KV：**10 万读/天、1 千写/天、1 GB 存储**。
- 一个班（~50 人）：月用量是免费额度的万分之一 → **实质免费，无收费风险**。
- 注意：KV"同一 key 写限 1 次/秒"；手动派发远低于此，批量派发需串行/限流。

### 8.3 与方案 X 的取舍（已弃用 X）
- X 写 GitHub 仓库触发重建：延迟 1–2 分钟，且 Function 需持"能改仓库"的 GitHub token（高价值秘密）。
- Y 写 KV：即时、Function 只需公钥+KV 绑定（无外部高权限 token）→ **Y 更优**，故选 Y。

---

## 9. 实施清单（文件级，待落地）

学生站（cs-a、ss-a 各一套，结构相同）：
- [x] 新建 `functions/api/certs.js`：`GET` 读 KV 返回名册（已实现，含 CORS + 30s 缓存）。
- [x] 新建 `functions/api/issue.js`：`POST` 验签 → 合并写 KV（已实现，多公钥试签、CORS）。
- [ ] 在 Cloudflare 建 KV 命名空间并绑定 `CERTS_KV`；设 `TEACHER_PUBLIC_KEY` 变量（**需后台操作，见部署运行手册 §2**）。
- [x] `src/components/IdentityGate.jsx`：改为 `fetch('/api/certs')` + 合并打包名册兜底 + 多公钥逐把验签（失败降级已实现）。
- [x] `public.json` 改为「旧随机密钥 + 密码派生密钥」数组（首次引导见 §10 / 运行手册 §1）。
- [ ] 把现有 `src/data/certs.json` 内容**种子导入** KV（可选；学生站已兜底打包名册，见运行手册 §2.3）。

教师端（tc-a）：
- [x] 面板①新增"签发并发布"：密码输入 + 课程站选择 + 名册输入 + 浏览器 PBKDF2 派生 + 签名 + `POST /api/issue`。
- [x] 复用现有"保存激活码库"（本地持有 code 供 HMAC 核验）。
- [x] `src/lib/identity.js`（三站同源）增加 `deriveKeyFromPassword` + `publicJwkFromPrivate` + `asKeyArray` + `findTeacherKey`。
- [x] 新增 `tools/bootstrap.mjs`：密码派生公钥写三站 public.json + 打印 `TEACHER_PUBLIC_KEY` + 生成 `kv-seed.json`。

通用：
- [x] 仓库内提交 `SALT` 常量（`KDF_SALT='xue-liang-ce-2026-salt'`，公开）。
- [x] 文档与代码同步（技术设计 + 部署运行手册 `身份指纹系统_部署运行手册.md`）。

> 实现状态（2026-08-18）：**代码全部落地并通过本地 crypto 自测（派生/签名/验签/确定性/多密钥定位均 OK）与三站 `npm run build` 验证。** 剩余仅 Cloudflare 后台的 KV 绑定 + 环境变量设置 + 重部署（运行手册 §2–§3），以及可选 KV 种子。

---

## 10. 首次公钥引导（一次性部署步骤）

1. 老师首次设定密码 `P`。
2. 本地/脚本用 `deriveKeyFromPassword(P, SALT)` 得公钥 `pub`。
3. 把 `pub` 写入：cs-a/ss-a 的 `src/data/public.json`、tc-a 的 `src/data/public.json`、`TEACHER_PUBLIC_KEY` 环境变量（Cloudflare 后台）。
4. 部署学生站 + 教师端。
5. 之后日常派发纯网页，无需再动部署。

> 这步仍需一次部署，是因为公钥必须进入学生站应用与 Function。之后日常发码不再部署。

---

## 11. 局限与注意

- **同学生多设备激活**：身份锁在每台设备 IndexedDB，清缓存/换浏览器可再激活。**零后端架构下无法禁止**（需服务端才能强制唯一）——与现状一致，属可接受的已知限制。
- **KV 最终一致**：写入后边缘传播通常亚秒~数十秒；同区域即时。学生偶发拉到旧名册可重试/等待。
- **密码=根信任**：丢失密码无法签发新码（已激活学生不受影响）；改密码=换密钥=重部署公钥。
- **名册可用性**：KV 被清空会导致学生暂时查不到证书；建议保留 KV 版本/备份或定期从仓库种子恢复。

---

## 12. 复用性说明（通用方案）

本方案是**通用模式**，可脱离"学练测平台"独立复用，适用于任何：
- 零后端静态站 + 需要"老师/管理员签发、用户激活"的场景；
- 要求防冒用（Ed25519 验签）、防篡改（HMAC）、且签发要在浏览器完成；
- 希望学生端不因新增用户而重部署。

核心可复用单元：
1. `identity.js`：Ed25519 签名/验签 + HMAC 绑定（浏览器/Node 通用）。
2. 密码派生密钥工具：`deriveKeyFromPassword`（PBKDF2 + 固定 SALT）。
3. Pages Function 模板：`GET /api/certs` + `POST /api/issue`（验签后写 KV）。
4. 学生端 `fetch` 远程名册 + `verifyCert` 的激活范式。

建议：本方案可进一步沉淀为 **WorkBuddy Skill**（如 `zero-backend-signed-enrollment`），便于其他项目一键套用。

---

## 13. 后续步骤

1. 确认文档无误 → 按 §9 清单落地实现（先 cs-a 单站跑通，再镜像 ss-a）。
2. 端到端实测：老师网页发码 → 学生秒级激活 → 导出 → 教师端核验。
3. （可选）将本方案封装为 WorkBuddy Skill 供复用。
