# 需求补充规格 v0.2（自适应学习闭环）

> 本文档是 `TECH-SPEC.md` 的**增补章节**，描述在"通用模板"方向下新增的三层能力：
> **单元前/后测（诊断+效果评估）**、**阅读中交互检查点（高密度反馈）**、**探索驱动+趣味挑战（激发兴趣、强化记忆）**。
>
> - 版本：v0.2
> - 日期：2026-08-13
> - 关联文档：`TECH-SPEC.md` / `QUICK-START.md` / `CONTENT-TEMPLATE.md` / `TRAPS-GUIDE.md`

---

## 0. 设计原则

两层理论贯穿全部新增需求：

1. **刻意练习框架（Ericsson）**
   - 明确、可测的目标；任务难度处于"能力边缘"；
   - **即时反馈**（边学边校正）；重复 + 反思；逐步构建"心理表征"。
2. **学习梯度理论（最近发展区 / 认知负荷 + 难度递进）**
   - 内容沿 **易 → 中 → 难** 梯度展开；
   - 前测定位学生起点，后测确认达成，挑战任务在最近发展区内向上延伸（脚手架）。

---

## 1. 总体闭环

```
单元前测(诊断现状) → 学习+交互检查点(高密度反馈/主动回忆)
   → 探索 & 挑战(梯度·趣味) → 单元后测(测掌握度)
   → 效果分析(学习增益) → 自适应下一步(复习/进阶/挑战) ↺
```

五个阶段构成可回流的闭环：诊断建立基线，学习过程持续反馈，后测量化增益，增益驱动自适应分流。

---

## 2. 需求 A：单元前/后测（诊断 + 效果评估）

### 2.1 目的
- **前测**：进入单元前摸清学生"当前状态"（先验知识、常见误区），建立心理表征基线。
- **后测**：单元结束后测"掌握度"。
- **效果量化**：`学习增益 = 后测得分 − 前测得分`，判断学习是否有效。

### 2.2 可复用（现有引擎）
- 题型体系（`multiple_choice` / `fill_blank` / `essay` 及编程题）。
- `judge` 判题逻辑、`exam_records` 存储与离线同步队列模式。

### 2.3 内容模型
- 路径：`public/data/assessments/[unit-slug].json`
```json
{
  "unitId": "u01",
  "pre":  [ { "id": "u01_p1", "type": "multiple_choice", "title": "...", "instruction": "...", "testConfig": { "expected": "B", "options": ["A","B","C"] } } ],
  "post": [ { "id": "u01_q1", "type": "multiple_choice", "title": "...", "instruction": "...", "testConfig": { "expected": "C", "options": ["A","B","C"] } } ]
}
```

### 2.4 数据模型（新增表）
```sql
CREATE TABLE assessment_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    unit_id TEXT NOT NULL,
    phase TEXT NOT NULL,            -- 'pre' | 'post'
    score INTEGER NOT NULL,
    answers TEXT NOT NULL,          -- JSON
    completed_at INTEGER NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 2.5 API（新增 / 扩展）
| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/assessment` | GET | 取某 unit 的 pre/post 题目（`?unitId=`） |
| `/api/assessment` | POST | 提交 pre/post 答案 `{ unitId, phase, answers }` |
| `/api/assessment/status` | GET | 返回 `{ hasPre, hasPost, preScore, postScore, gain }` |
| `/api/stats` | GET | **扩展** `learningGain`、`perUnitMastery` 维度 |

### 2.6 流程
1. 打开单元 → 查询 `assessment/status`。
2. 无 pre 记录 → 弹**诊断前测**（低利害，可跳过）。
3. 正常学习（含检查点、探索、挑战）。
4. 单元末 → **后测**（计入掌握度）。
5. 展示**学习增益**与下一步建议（复习 / 进阶 / 挑战）。

### 2.7 理论挂钩
- 前测 = 建立心理表征基线（刻意练习）。
- 后测 = 测有意练习成果；可延迟重做实现**间隔检索**（spacing）。

---

## 3. 需求 B：阅读中交互检查点（高密度反馈）

### 3.1 目的
- 学习教程时按**合理密度**嵌入轻量互动（预测、快选、填空），**即时反馈**。
- 边读边"主动回忆"，把被动阅读转为主动建构。

### 3.2 可复用
- 题型、`judge`、IndexedDB、Markdown 渲染管线（`LearnChapter.tsx`）。

### 3.3 内容语法（Markdown 扩展指令）
通过 `react-markdown` 自定义组件解析：
```markdown
:::checkpoint{type="predict" question="这段循环会输出几次？" options=["1","3","无限"] answer="3" feedback="i 从 0 到 2，共 3 次"}
```
```markdown
:::checkpoint{type="fill" question="补全：列表的索引从 ___ 开始" answer="0" feedback="大多数语言列表索引从 0 开始"}
```

### 3.4 运行时（新增组件）
- `src/components/InteractiveCheckpoint.tsx`：内联渲染、即时反馈、可重试。
- 写入 `checkpoint_records`，纳入离线同步队列。

### 3.5 数据模型（新增表）
```sql
CREATE TABLE checkpoint_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    unit_id TEXT NOT NULL,
    item_id TEXT NOT NULL,          -- checkpoint / challenge 的 id
    kind TEXT NOT NULL,             -- 'checkpoint' | 'challenge'
    correct INTEGER NOT NULL,       -- 0/1（最近一次）
    attempts INTEGER NOT NULL,
    last_at INTEGER NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 3.6 密度规则（硬性，防泛滥）
- 每 **3–5 分钟阅读 / 每个知识点** 至多 1 个检查点。
- 单单元设上限（建议 ≤ 单元知识点数）。
- 必须：**与当前内容强相关、简单有趣、零惩罚**（答错仅给反馈，不计负分）。

### 3.7 理论挂钩
- 阅读中检索 = **主动回忆**（刻意练习核心机制）。
- 即时反馈 = 练习—反馈闭环；检查点难度沿**学习梯度**由易到难。

---

## 4. 需求 C：探索驱动 + 趣味挑战（激发兴趣、强化记忆）

### 4.1 目的
- 内容本身诱发**探索欲**：走"探索分支 + 挑战任务"，在趣味与挑战中固化记忆。
- 探索内部自带互动；挑战置于"能力边缘"，供高水平者延伸。

### 4.2 可复用
- 内容 JSON / Markdown 体系、检查点基础设施、题型。

### 4.3 内容模型
- unit 可含 `explorations[]`（可选分支，内部自带 `:::checkpoint`）与 `challenges[]`（更难可选任务）。
- Markdown 指令：
```markdown
:::explore{title="动手试：" body="把步长改成 2，输出会怎样？"}
:::challenge{id="u01_c1" type="output" title="进阶挑战" instruction="..." testConfig={"expected":"..."}}
```

### 4.4 游戏化（可选层）
```sql
CREATE TABLE progress (
    user_id TEXT PRIMARY KEY,
    xp INTEGER NOT NULL DEFAULT 0,
    streak INTEGER NOT NULL DEFAULT 0,
    badges TEXT NOT NULL DEFAULT '[]',   -- JSON
    updated_at INTEGER NOT NULL
);
```
- Profile 展示 XP / 连胜 / 徽章；以**正反馈为主**，不制造焦虑。

### 4.5 学习梯度注入
- 每个单元声明难度梯度（易 → 中 → 难）。
- 前测定位起点，后测确认，挑战任务向上延伸（最近发展区脚手架）。

### 4.6 理论挂钩
- 挑战 = "能力边缘"难度（刻意练习的恰当难度）。
- 探索 + 互动 = **交错 / 变式练习**；趣味降低认知负荷、提升留存。

---

## 5. 结构性改动汇总

1. **课程结构细化**：`chapter` 下新增 `units[]`，**unit 成为最小闭环单元**（承载 前测 / 检查点 / 探索 / 挑战 / 后测）。`chapter` 保留为导航与分组层。
2. **内容模板扩展**（`CONTENT-TEMPLATE.md` 增补）：
   - unit 模板（含 `preAssessment` / `checkpoints` / `explorations` / `challenges` / `postAssessment`）。
   - assessment 模板（`assessments/[unit-slug].json`）。
   - `:::checkpoint` / `:::explore` / `:::challenge` 指令模板与编写规范。
3. **作者规范（必需）**：因三条需求都依赖作者撰写检查点/探索内容，必须提供"密度 + 梯度"编写指南，否则易泛滥或缺失。

---

## 6. 默认假设（4 个开放点的暂定默认值，待最终确认）

| # | 开放点 | 默认建议 |
|---|--------|----------|
| 1 | 单元粒度 | `chapter` 下加 `units[]`；unit 为最小闭环单元，chapter 仅作分组/导航 |
| 2 | 前测属性 | **低利害、可跳过、不计入总成绩**，但计入个人诊断画像；后测计入掌握度 |
| 3 | 检查点题型 | 默认轻量题（选择/预测/填空/简答）；**编程课允许** `output`/`function` 代码题作为检查点 |
| 4 | 游戏化强度 | 默认"即时正反馈 + 可选游戏化层（XP/徽章/连胜）"，**默认开启、可一键关闭** |

> 以上为暂定默认值，最终以用户确认为准；确认后写入 `MEMORY.md` 项目约定。

---

## 7. 待确认与后续

- [ ] 确认第 6 节 4 项默认值（或给出调整）。
- [ ] 确认是否进入**脚手架设计**（数据迁移脚本、API、前端组件拆分）。
- [ ] 提供 1 个示例单元（含前/后测 + 2~3 检查点 + 1 探索 + 1 挑战）作为内容模板范本。
- [ ] 评估对 TRAPS-GUIDE 的影响：新增表需在 `init-exam-db` / 迁移脚本中一并建表；检查点/探索不改变时区、草稿 key 等已有坑位。

**文档结束**
