# 学练测一体化平台 · 项目总览与交接文档

> 适用：新对话接管本项目时先读这份。配套每日工作日志在 `.workbuddy/memory/2026-08-*.md`，长期约定见 `.workbuddy/memory/MEMORY.md`。
> 最近更新：2026-08-18（本轮完成测试中心 bug 验证 + 指纹身份系统端到端复核）。

---

## 0. 一句话定位

一个**「学—练—测」一体化**的网页学习平台（零后端、纯前端 + IndexedDB 本地存储），用于教材拆解成情境剧式课程、配套三层测验、并用**密码学身份指纹**防止学生冒用/篡改成绩导出。按业务拆成**三个独立站点**部署到 Cloudflare Pages。

---

## 1. 三站架构总览（各自独立 git 仓库、独立子域名）

| 站点 | 目录 | 仓库远程 | 内容 | 部署状态 |
|------|------|----------|------|----------|
| **cs-a**（主站·供应链） | `learning-platform/` | `cao-lg/cs-a.git` | 1 个项目 `supply-chain`，33 单元 | 已部署，Cloudflare Pages 自动部署 |
| **ss-a**（销售站） | `sales-platform/` | `cao-lg/ss-a.git` | 6 个项目 `sales-project1~6`，49 套 assessment、28 套 exam | 已部署 → `https://ss-a.pages.dev` |
| **tc-a**（教师端控制台） | `teacher-console/` | `cao-lg/tc-a.git` | 老师本地优先工具：发码/核验/分析 | 代码已推，**待 Cloudflare 建站**才能网页访问 |

- `learning-platform/` 是父仓库 `chain_supply/.git` 的**子目录**（不是独立仓库）；`sales-platform/`、`teacher-console/` 是**独立 git 仓库**（已加入父仓库 `.gitignore`）。
- 三站 IndexedDB 按 origin 天然隔离，互不干扰。
- ⚠️ `D:\` 根另有无关 stray 仓库（remote=Python-V0.git），与本工程无关，勿混淆。

---

## 2. 技术栈与设计系统

- **技术栈**：Vite5 + React18 + React Router6(HashRouter) + framer-motion11 + highlight.js + idb-keyval（IndexedDB）。
- **设计系统**：暖珊瑚 `#ff8a5b` 单色强调 + 薄荷 `#7fe1c4`；底色深暖紫墨 `#171225` + 三层彩色辉光渐变；文字暖米白 `#f4ece0` / 暖灰紫 `#bcaece`。全链路 `prefers-reduced-motion` 双重降级（CSS 媒体查询 + JS detect）。
- **明暗主题**：`:root[data-theme="light"]` + Layout ☀️/🌙 toggle（localStorage 持久化）；块级 theme `.viz[data-theme="coral|mint|amber|violet"]` 覆盖局部变量，由解析器透传 `{...b.attrs}`，无需改解析器。

---

## 3. 内容范式（教材 → 整站 流水线）

- 每单元一篇 `public/data/courses/<id>.md`，由 `src/lib/mdParser.js` 通用解析任意 `:::xxx{}` 容器块（兼容 LF/CRLF，qchain/question 递归解析 body）。
- **情境/可视化指令**：`:::scene`（情境剧）、`:::kpi`/`:::funnel`/`:::flow`/`:::formula`（指标卡/漏斗/流程链/公式，解决大段文字枯燥）、`:::checkpoint`/`:::explore`/`:::challenge`（自测/探索/挑战）。
- **问题链（全程互动）**：`:::qchain` → 多个 `:::question`（提问 → 点开 `:::reveal` 看解析 → 确认「我明白了」→ 解锁下一问，顺序解锁；question 顶层须有验证门 checkpoint/challenge，不能塞进 reveal 内）。体内可嵌套任意可视化块。
- **题库独立**：`public/data/assessments/<id>.json`，结构 `{pre:{items},post:{items}}`（单/多题统一），`LearnUnit` 用 `extractItems()` 归一化。
- **路线页**：`GrowthJourney` = 左侧粘性目录 `CourseSidebar` + `CourseShell`，桌面两栏 / 移动抽屉，按项目分组展示单元完成状态。
- **拆解流水线**：教材 → curriculum-decomposer 技能 → `courses/*.md` + `assessments/*.json` + `courses/*.json` → LearnUnit/CourseDetail/GrowthJourney 自动渲染。缺口：拆解仍靠 AI 执行一次；多用户需把 storage/api 层换 Cloudflare Workers+D1（已抽象隔离）。

---

## 4. 五大能力模块

### 4.1 测验中心（三层测验体系）
- **单元测试**：每任务一张卷，复用该任务 `assessments/<id>.json` 的 **post** 题库，`buildExamVariant` 随机抽卷 + 选项乱序。路由 `/test/unit/:courseId/:unitId`。
- **项目综合测试**：每个项目的章节阶段考（`exams/chN.json`）。
- **结业大考**：`exams/final|final-2|final-3.json`。
- **统一入口 `TestHub.jsx`** 路由 `/tests/:courseId`：顶部「项目切换条」`.project-switch`（pills 列出 manifest 全部课程，当前高亮，点击切 `/tests/:id`；`allCourses.length<=1` 时自动隐藏——cs-a 单课程不显示）。

### 4.2 身份指纹系统（防冒用 / 防篡改）🌟 重点
- **密码学底座** `src/lib/identity.js`（三站同源复用）：
  - **Ed25519 证书**：老师用私钥签发 `cert = sign(priv, 学号|姓名|激活码)`，公钥内置 App 验证书确由老师签发，无法伪造新身份。
  - **HMAC-SHA256 防篡改**：导出时用 `k = SHA256(激活码)` 对全量记录算 `bundleMac`，把记录「绑死」在身份上。
- **学生端**：`IdentityGate.jsx` 输入 学号+姓名+激活码 → `verifyCert` 校验证书签名通过即锁定本机身份。
- **老师核验**：`VerifyPanel.jsx` 注入激活码校验归属与真伪；`storage.exportLearnerData` 导出含 `identity+mac`，导入时归属不符保留原主标记并提示。
- **教师端跨站**：`teacher-console/src/verify.js` 内置**双公钥数组**逐把尝试（cs-a 公钥 `emEN…` + ss-a 公钥 `gGyt…`），已签发学生可直接跨站核验。
- **公开产物**：各学生站 `src/data/certs.json`（已签发证书）+ `src/data/public.json`（公钥）；教师端 `tools/` 另有 `issue-codes.mjs`（发码/重签）、`verify-export.mjs`（命令行批量核验）。
- 🔴 **密钥红线**：`tools/teacher-keys.json`（Ed25519 私钥）与 `tools/teacher-secrets.json`（激活码）已 `.gitignore`，**绝不入库**；伪造激活码 / 错密钥 / 篡改记录校验均返回 false。

### 4.3 游戏化 / 愉悦层（DelightfulExperienceDesigner 视角）
- `src/lib/gamify.js` 纯函数引擎：等级 `TIERS`（10 级）+ `XP_PER_LEVEL=150`；`BADGES` 6 类（first-step/streak-3/streak-7/perfect/halfway/graduation）；`streakBump` 真实连击（同天不重复 / 隔日+1 / 中断归 1）；`evaluateBadges`/`celebrationFor`。
- `Celebration.jsx` 多步状态机（entry→xp→level→badges→finale + StepDots + XpChip + Confetti 44 片，reduce 不自动轮播）；`FeedbackEffects.jsx` 共享原语 `GainFloat`/`SparkleBurst`/`SuccessPulse`；`copy.js` 微文案库 MICROCOPY。
- 全部动效尊重 `prefers-reduced-motion`（CSS + JS detect 双重降级）。

### 4.4 学习行为记录（IndexedDB，keyval-store/keyval）
已记录（`:v4`）：用户身份、课前/课后测整卷+每题对错 graded+时间戳、检查点/挑战对错+尝试次数、进度 XP/streak/徽章、**学习时长**（按单元+按天累加，每 10s 落盘 + 隐藏暂停 + 卸载 flush）、**测验重做历史**（append，保留最新 10 次，不覆盖）、**错题本聚合**（读 post.graded 错题 + join 题库补全题干/选项/正确答案，Profile 展示）。进度结构：`xp / streakDays / lastDay / badges[]`，等级由 `levelInfo(xp)` 派生、徽章由 `evaluateBadges` 解算。

### 4.5 教师端控制台（tc-a，零后端零上传）
三面板：① `issue-codes.mjs` 发放激活码/证书（复用学生站同密钥，始终重签保证一致）② 批量导入学生导出文件，公钥验归属 + 激活码验 HMAC 防篡改（篡改标红剔除）+ 已核验明细（学号/姓名/脱敏激活码/成绩/XP/等级/时长/错题）③ 可视化分析（概览卡 / 成绩分布 / 综合排名 / 高频错点 / 明细 / 导出 CSV / 邮件发送报表）。私钥与激活码只存本机 `tools/`（gitignore），前端仅内置公钥 + 公开证书。支持跨设备导入/导出（打包激活码库 + 已导入文件 + 邮件配置迁移）。

---

## 5. 部署 / 构建 / 代码托管约定（务必遵守）

- **dev 端口**常驻 `:5180`（5173/5174/5175 历史占用）。
- **`npm run build`** 需 `dangerouslyDisableSandbox:true`（沙箱拦写 dist）；vite build 自带清空 outDir，**勿手动 `rm -rf dist`**（预览服务占用会报 Device or resource busy）。管道接 `tail` 偶发 SIGPIPE 致 exit 1，改用 `cat` 看输出。
- **Git 推送红线**：三站 remote 保持 **token-free**；推送用一次性内联 token `git push https://<token>@github.com/cao-lg/<repo>.git main`，**绝不把 token 写进 remote URL**。
  - cs-a：push 父仓库 `chain_supply` 的 main（在父仓内 `git add` 须显式指定文件，**勿 `git add -A`**——会扫入 `_textbook/`/`初稿6.18/` 二进制）。
  - ss-a / tc-a：分别进各自子目录仓库 push。
- **Cloudflare Pages**：cs-a、ss-a 已建项目连各自仓库（构建 `npm run build` / 输出 `dist`）自动部署；tc-a 仍需**新建项目**连 `cao-lg/tc-a` 才能网页访问。

---

## 6. 已知调试坑（可复用）

1. `defaultCourseId()` 取 `manifest.courses[0]`，而 `manifest.courses` 是**课程 id 字符串数组**（如 `["supply-chain",...]`），非 `{id}` 对象——勿写 `?.id`。
2. 本应用 IndexedDB 实际为 **DB `keyval-store`、仓库 `keyval`**（idb-keyval 默认 store 经 createStore 重命名），注入模拟数据须写该库否则读不到。
3. agent-browser `open` 相同 URL 时 SPA 不重载沿用旧状态，验证状态变化须 `location.reload()` 或换新端口；其文本/引用点击不稳定，弹窗类 E2E 建议用 `eval` 写 IndexedDB 模拟或 Node ESM 直驱真实模块。
4. 改 `src/lib/exam.js` 后**两站必须同步**（脚本 import 相对位置，单站改会致另一站跑旧逻辑）。
5. 测验抽卷 `buildExamVariant` 保证「每单元至少 1 题」再随机补足；`gen_comprehensive.mjs` 的 `pick` 按单元数算（cs-a=53 / ss-a=79）。

---

## 7. 当前状态 / 待办

### 已完成
- 三站代码、身份指纹系统、三层测验、游戏化层、学习行为记录、教师端控制台均落地并构建 0 error。
- 测试中心「冷启动并行 fetch 竞态导致部分项目误报『暂未上线』」bug 已修（api.js 加 `fetchJSON` 3 次退避重试 + getExam/getAssessment 降级），推送 ss-a `f977c94` / cs-a `9f9bc4f` / tc-a `a8f8b46`。
- 指纹身份系统端到端实测通过（学生激活门 + 教师跨站核验 + 防篡改/防伪造 + 教师签发 round-trip，12/12 断言通过）。

### 🔴 待办（优先）
1. **GitHub token 安全**：`ghp_YXPn7…`（明文已从文档移除）已暴露且前序多次用于推送，**必须到 GitHub 撤销并轮换**，轮换后三站 remote 保持 token-free。
2. **tc-a 上线**：在 Cloudflare Pages 新建项目连 `cao-lg/tc-a`（构建 `npm run build` / 输出 `dist`）获独立子域。

### 可选未做
- 学习行为记录仍可补：阅读/滚动轨迹、explore/scene 互动事件。
- 多用户 / 服务端监考需把 storage/api 层换 Cloudflare Workers+D1（已抽象隔离，但本期不做了——纯前端方案精准解决「冒用/篡改归属」，无法证明学员真正完成学习）。
